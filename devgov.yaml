# Generated by devgov init — edit intentionally; see docs/policy_config.md
# contract_compiler.enabled true; empty critical_surfaces.paths until law aligned.
# `devgov contract generate` uses automatic git diff when applicable; `--no-diff` forces bootstrap (no_graph_context). `devgov plan --emit-contract` uses pipeline-class scope.
version: 1
project:
  name: "gravity-ho-matey"
  language_profile: "python"
profiles:
  default: dev
  available:
    - dev
    - ci
critical_surfaces:
  paths: []
contract_compiler:
  enabled: true
  strict_scope_lock_default: true
  classifier: {}
